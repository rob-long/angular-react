import angular from 'angular';
import './angular-app';
import './react-directive';
import './AppBridge';

// Initialize the shared state
const initialState = {
  text: 'Initial text',
  items: [1, 2, 3]
};

