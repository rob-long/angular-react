import angular from 'angular';
import app from './angular-app';
import './react-directive';
import './style.css'; // Your styles, if any

// Manually bootstrap AngularJS if needed
angular.bootstrap(document, ['myApp']);
